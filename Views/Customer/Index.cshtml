@model IEnumerable<Customer>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary"> قائمة العملاء</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Customer" asp-action="New" class="btn btn-primary">
                <i class="bi bi-file-plus"></i> &nbsp; اضافة عميل جديد
            </a>
        </div>
        <div class="row">
            <form class="d-flex">
                <input name="searchString" class="form-control me-1" type="search" placeholder="Search" aria-label="Search"></input>
                <button class="btn btn-dark" type="submit"> Search by name</button>
            </form>

        </div>

    </div>
    <table class="table table-hover" id="tree-table">
        <thead>
            <tr>
                <th scope="col">اسم العميل</th>
                <th scope="col">رقم البطاقة</th>
                <th scope="col">رقم التليفون</th>
                <th scope="col"></th>

            </tr>
        </thead>
        <tbody>
            @foreach (var Customer in Model)
            {

                <tr class="table-primary">
                    <td scope="row" width="50%">
                        @Customer.CustomerName
                    </td>
                    <td width="50">@Customer.IdNo</td>
                    <td width="50">@Customer.Phone</td>
                    <td>

                        <a asp-controller="Customer" asp-action="Edit" asp-route-CustomerId="@Customer.CustomerId" class="btn btn-success">
                            <i class="bi bi-pencil-square"></i>
                        </a>


                        &nbsp;
                        <a asp-controller="Customer" asp-action="Delete" asp-route-CustomerId="@Customer.CustomerId" class="btn btn-danger">
                            <i class="bi bi-trash"></i>
                        </a>


                    </td>

                </tr>
            }
        </tbody>


    </table>
</div>
